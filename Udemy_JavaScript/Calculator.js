var firstNumber = prompt("Enter First Number");	
var secondNumber = prompt("Enter Second Number");
var	operater = prompt("Enter an operater");
//var output = error
if (operater = "+") {
	var otput == parseInt(firstNumber) + parseInt(secondNumber);
	alert(otput);
};
if (operater = "-") {
	var oput == parseInt(firstNumber) - parseInt(secondNumber);
	alert(oput);
};
if (operater = "*") {
	var utput == parseInt(firstNumber) * parseInt(secondNumber);
	alert(utput);
};
if (operater = "/") {
	var ouput == parseInt(firstNumber) / parseInt(secondNumber);=
	alert(ouput);
};
 
